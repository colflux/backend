# Estilos compartidos

Ubica aqui los archivos CSS reutilizables del portal. En una siguiente
iteracion, los estilos embebidos en cada prototipo pueden extraerse a
`base.css` y a un archivo por pagina.
